package Java1课本示例代码.Chapter04.Example4_13;

public class Student {
    double computeArea(Circle circle) {
        double area = circle.getArea();
        return area;
    }

    double computeArea(Tixing tixing) {
        double area = tixing.getArea();
        return area;
    }
}
